const info = document.getElementById('info')
info.innerText = `This app is using chrome (version: ${window.versions.chrome()}), Node js (version: ${window.versions.node()}), Electron (version: ${window.versions.electron()})`